query Me {
  me {
    id
    discordId
    username
    role
    servers {
      id
      createdDate
      updatedDate
      name
      pid
      displayName
      playerCount
      maxPlayerCount
      startedDate
    }
  }
}
